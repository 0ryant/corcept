---
title: Corcept identity manifest
tags: [corcept, release, rename, governance-runtime, claude-code]
seo:
  title: Corcept fullship release
  description: Corcept is the shipped identity for the governance runtime package.
---

# Corcept identity manifest

This archive is the Corcept fullship package. It preserves the governance/runtime scaffold while using Corcept naming across the release.

Renamed surfaces include:

- repository root
- Rust crate names and imports
- Claude Code plugin namespace
- skills, agents, hooks, and hook binaries
- project state directory `.corcept/`
- CLI and environment naming
- benchmark/eval package naming
- nested distribution zips
- docs, metadata, SEO tags, and search tags
