# External adapters

These adapters are intentionally command-template based rather than locked to one agent. CORCEPT is a runtime/plugin layer, so the fair experiment is to run exactly the same model/agent command twice: once without CORCEPT, once with CORCEPT loaded.

Use `python -m corcept_eval preflight` to see which harness dependencies are present.
