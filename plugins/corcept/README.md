---
title: Corcept Plugin
description: Claude Code plugin for CORCEPT governed runtime.
seo:
  title: Corcept Plugin - Claude Code hooks, skills, and agents
  description: Claude Code plugin scaffold containing CORCEPT skills, agents, lifecycle hooks, and CLI wrappers.
  keywords: [Claude Code plugin, Corcept, hooks, skills, agents, governance]
tags: [plugin, claude-code, hooks, skills, agents, corcept]
status: complete
---

# Corcept Plugin

Load locally with:

```bash
claude --plugin-dir ./plugins/corcept
```

The plugin expects `corcept` to be available on PATH. Build and install the CLI with:

```bash
cargo install --path crates/corcept-cli
```
