# Hook fixtures

JSON inputs for `corcept-runtime` hook integration tests. Placeholders:

- `__CWD__` — replaced at test time with the temp project root.

Each fixture validates against `contracts/schemas/corcept-hook-input-v1.schema.json`.
